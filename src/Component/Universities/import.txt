import {
  Button,
  Paper,
  Table,
  TableBody,
  TableCell,
  TableContainer,
  TableRow,
  TextField,
  Typography,
} from "@mui/material";
import { Box } from "@mui/system";
import React, { useEffect, useState } from "react";
import { Link, useParams } from "react-router-dom";
import ArrowRightAltIcon from "@mui/icons-material/ArrowRightAlt";
import AccountBalanceIcon from "@mui/icons-material/AccountBalance";

const Universities = () => {
  const [universities, setUniversities] = useState([]);
  const [showData, setShowData] = useState(20);
  const { countryName } = useParams();

  useEffect(() => {
    fetch(`http://universities.hipolabs.com/search?country=${countryName}`)
      .then((res) => res.json())
      .then((data) => setUniversities(data.slice(0, showData)));
  }, []);

  console.log(showData);
  return (
    <>
      <Box
        style={{ background: "#262626" }}
        sx={{ textAlign: "center", py: "3rem" }}
      >
        <Box>
          {/*      <TextField
              label="Standard"
              variant="standard"
              inputProps={{ sx: { color: "#fff" } }}
            /> */}
          <Typography variant="h5" color="white" mt={2}>
            See Below University list if don't then search it.
          </Typography>
        </Box>
        <Link to="/home" title="Go back home">
          <ArrowRightAltIcon sx={{ fontSize: "3rem", color: "white" }} />
        </Link>
      </Box>
      <Paper style={{ margin: "30px 25px" }}>
        <TableContainer>
          <Table>
            <thead>
              <th>Name</th>
              <th>Country</th>
              <th>Details</th>
              <th>Domains</th>
            </thead>
          </Table>
          <TableBody>
            <TableRow>
              <TableCell>Hell</TableCell>
            </TableRow>
          </TableBody>
        </TableContainer>
      </Paper>
    </>
  );
};

export default Universities;
